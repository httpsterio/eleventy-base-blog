<!doctype html>
<html lang="{{ metadata.language }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title }}</title>
    <meta name="description" content="{{ description or metadata.description }}">

    {#- Uncomment this if you’d like folks to know that you used Eleventy to build your site!  #}
    <meta name="generator" content="{{ eleventy.generator }}">

    <link rel="stylesheet" href="{{ '/assets/index.css' | url }}">
    {# <link rel="stylesheet" href="{{ '/assets/prism-base16-monokai.dark.css' | url }}">
    <link rel="stylesheet" href="{{ '/assets/prism-diff.css' | url }}"> #}
    {# <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}"> #}

    {# <link rel="alternate" href="{{ metadata.jsonfeed.path | url }}" type="application/json" title="{{ metadata.title }}"> #}
    <link rel="stylesheet" href="/assets/photoswipe.css">
  </head>
  <body>
    <header>
      <h1>Hennan ja Samin häät</h1>
    </header>

    <main{% if templateClass %} class="{{ templateClass }}"{% endif %}>
      {{ content | safe }}
    </main>

    <footer></footer>

    <!-- Current page: {{ page.url | url }} -->

<script type="module" src="/assets/index.js"></script>
  </body>
</html>
